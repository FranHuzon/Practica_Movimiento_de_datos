### 1. Importad el fichero resultante de la exportación completa de las tablas y los datos de una instancia de ORACLE
en otra instancia diferente empleando el comando impdp y explicad qué problemas surgen. 
Realizad un remapeo de esquemas si es necesario.

Al realizar una primero exportación vimos que daba una cantidad de fallos enormes, y averiguamos que no había que exportar el esquema de los usuarios del systema.

Primero, hemos exportado desde una base de datos rellena, la estructura de la misma con el comando:

~~~

~~~

